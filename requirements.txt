# FastAPI and ASGI server
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Data validation and serialization (using version with pre-compiled wheels)
pydantic==2.7.4

# HTTP client for testing (optional but recommended)
httpx==0.25.2
